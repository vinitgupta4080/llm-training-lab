# Experiment: Module 1 BPE merges

## Prediction

- Corpus: `banana banana`
- Chosen first pair under tie-break: `a+n`
- Predicted tokens for one `banana` after the first merge: learner supplied the count; exact
  sequence clarified as `[b, an, an, a]`.
- Predicted token count after the first merge: 4 (correct).
- Expected benefit:
- Expected cost:

## Design

- Independent variable: number of learned merges.
- Controls: corpus, starting byte vocabulary, tie-break rule, and evaluation text.
- Metrics: vocabulary size, token count, compression ratio, round-trip correctness.
- Required slices: training-domain text and text from a different script/domain.

## Results

- Merge table:
- Byte baseline versus BPE:
- Parameter-cost estimate:
- Cross-domain result:
- Failure analysis:
